import './styles.scss';

const Footer: React.FC = () => {
  return (
    <footer className={'footer'}>
      <section>
        <span>SocialCards</span>
      </section>
    </footer>
  );
};

export default Footer;
